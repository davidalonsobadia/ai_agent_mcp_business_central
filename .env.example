# Copy this file to .env and fill in your values. Never commit .env to Git.

# ---------------------------------------------------------------------------
# OpenAI (required for FastAPI agent)
# ---------------------------------------------------------------------------
OPENAI_API_KEY=sk-your-openai-api-key

# ---------------------------------------------------------------------------
# Business Central MCP
# ---------------------------------------------------------------------------
# Base URL: https://api.businesscentral.dynamics.com/v2.0/{tenant-id}/{environment}/api/v2.0
BC_URL_SERVER=https://api.businesscentral.dynamics.com/v2.0/YOUR_TENANT_ID/Sandbox/api/v2.0
# Company name (use the "name" field from BC, not displayName)
BC_COMPANY=YourCompanyName

# Auth: "azure_cli" (requires az login) or "client_credentials"
BC_AUTH_TYPE=client_credentials

# Required when BC_AUTH_TYPE=client_credentials
BC_TENANT_ID=your-azure-tenant-id
BC_CLIENT_ID=your-azure-app-client-id
BC_CLIENT_SECRET=your-azure-app-client-secret

# Optional: path to local MCP server build (when using client_credentials with local build)
# BC_LOCAL_SERVER_PATH=./mcp-business-central-local/build/index.js
